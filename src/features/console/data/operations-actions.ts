"use server";

import { revalidatePath } from "next/cache";
import { z } from "zod";

import { requireBusinessContext } from "@/lib/supabase/business";

import { invoiceDraftSchema, invoicePaymentStatusSchema, jobAssignmentsSchema, jobUpdateSchema, quoteDraftSchema, quoteStatusSchema, scheduleJobSchema, type DeleteActionResult, type InvoiceActionResult, type JobActionResult, type JobPhotoActionResult, type QuoteActionResult } from "./operations-contract";
import { createInvoice, createQuote, deleteInvoice, deleteJob, deleteJobPhoto, deleteQuote, finalizeInvoice, scheduleJob, updateInvoicePayment, updateJob, updateJobAssignments, updateQuoteStatus, uploadJobPhoto } from "./operations-repository";

function failure(error: unknown, fallback: string) { if (error instanceof z.ZodError) return "Check the details and try again."; if (error instanceof Error && /permission|owner|co-owner|authentication/i.test(error.message)) return "You do not have permission to make this change."; return fallback; }
const idSchema = z.uuid();
const manager = () => requireBusinessContext(["owner", "co_owner"]);

export async function saveQuoteAction(input: unknown): Promise<QuoteActionResult> { const parsed = quoteDraftSchema.safeParse(input); if (!parsed.success) return { ok: false, message: failure(parsed.error, "Quote details are invalid.") }; try { const quote = await createQuote(await manager(), parsed.data); revalidatePath("/"); return { ok: true, quote }; } catch (error) { console.error("Quote save failed", error); return { ok: false, message: failure(error, "The quote could not be saved.") }; } }
export async function updateQuoteStatusAction(id: string, status: unknown): Promise<QuoteActionResult> { const parsedId = idSchema.safeParse(id); const parsedStatus = quoteStatusSchema.safeParse(status); if (!parsedId.success || !parsedStatus.success) return { ok: false, message: "The quote update was invalid." }; try { const quote = await updateQuoteStatus(await manager(), parsedId.data, parsedStatus.data); revalidatePath("/"); return { ok: true, quote }; } catch (error) { return { ok: false, message: failure(error, "The quote could not be updated.") }; } }
export async function deleteQuoteAction(id: string): Promise<DeleteActionResult> { const parsed = idSchema.safeParse(id); if (!parsed.success) return { ok: false, message: "The quote identifier was invalid." }; try { const deletedId = await deleteQuote(await manager(), parsed.data); revalidatePath("/"); return { ok: true, id: deletedId }; } catch (error) { return { ok: false, message: failure(error, "The quote could not be deleted.") }; } }

export async function scheduleJobAction(input: unknown): Promise<JobActionResult> { const parsed = scheduleJobSchema.safeParse(input); if (!parsed.success) return { ok: false, message: failure(parsed.error, "Schedule details are invalid.") }; try { const job = await scheduleJob(await manager(), parsed.data); revalidatePath("/"); return { ok: true, job }; } catch (error) { return { ok: false, message: failure(error, "The job could not be scheduled.") }; } }
export async function updateJobAction(input: unknown): Promise<JobActionResult> { const parsed = jobUpdateSchema.safeParse(input); if (!parsed.success) return { ok: false, message: failure(parsed.error, "Job details are invalid.") }; try { const job = await updateJob(await manager(), parsed.data); revalidatePath("/"); return { ok: true, job }; } catch (error) { return { ok: false, message: failure(error, "The job could not be updated.") }; } }
export async function deleteJobAction(id: string): Promise<DeleteActionResult> { const parsed = idSchema.safeParse(id); if (!parsed.success) return { ok: false, message: "The job identifier was invalid." }; try { const deletedId = await deleteJob(await manager(), parsed.data); revalidatePath("/"); return { ok: true, id: deletedId }; } catch (error) { return { ok: false, message: failure(error, "The job could not be deleted.") }; } }
export async function updateJobAssignmentsAction(input: unknown): Promise<JobActionResult> { const parsed = jobAssignmentsSchema.safeParse(input); if (!parsed.success) return { ok: false, message: "The assignment selection was invalid." }; try { const job = await updateJobAssignments(await manager(), parsed.data); revalidatePath("/"); return { ok: true, job }; } catch (error) { return { ok: false, message: failure(error, "The job assignments could not be saved.") }; } }
export async function uploadJobPhotoAction(formData: FormData): Promise<JobPhotoActionResult> { const jobId = idSchema.safeParse(formData.get("jobId")); const caption = z.string().trim().max(500).safeParse(formData.get("caption") || ""); const file = formData.get("photo"); if (!jobId.success || !caption.success || !(file instanceof File) || !["image/jpeg", "image/png", "image/webp"].includes(file.type) || file.size < 1 || file.size > 10 * 1024 * 1024) return { ok: false, message: "Choose a JPG, PNG or WebP photo up to 10 MB." }; try { const photo = await uploadJobPhoto(await requireBusinessContext(), jobId.data, file, caption.data); revalidatePath("/"); return { ok: true, photo }; } catch (error) { return { ok: false, message: failure(error, "The photo could not be uploaded.") }; } }
export async function deleteJobPhotoAction(jobId: string, photoId: string): Promise<DeleteActionResult> { const parsedJobId = idSchema.safeParse(jobId); const parsedPhotoId = idSchema.safeParse(photoId); if (!parsedJobId.success || !parsedPhotoId.success) return { ok: false, message: "The photo identifier was invalid." }; try { const deletedId = await deleteJobPhoto(await requireBusinessContext(), parsedJobId.data, parsedPhotoId.data); revalidatePath("/"); return { ok: true, id: deletedId }; } catch (error) { return { ok: false, message: failure(error, "The photo could not be deleted.") }; } }

export async function saveInvoiceAction(input: unknown): Promise<InvoiceActionResult> { const parsed = invoiceDraftSchema.safeParse(input); if (!parsed.success) return { ok: false, message: failure(parsed.error, "Invoice details are invalid.") }; try { const invoice = await createInvoice(await manager(), parsed.data); revalidatePath("/"); return { ok: true, invoice }; } catch (error) { return { ok: false, message: failure(error, "The invoice could not be saved.") }; } }
export async function updateInvoicePaymentAction(id: string, status: unknown): Promise<InvoiceActionResult> { const parsedId = idSchema.safeParse(id); const parsedStatus = invoicePaymentStatusSchema.safeParse(status); if (!parsedId.success || !parsedStatus.success) return { ok: false, message: "The invoice update was invalid." }; try { const invoice = await updateInvoicePayment(await manager(), parsedId.data, parsedStatus.data); revalidatePath("/"); return { ok: true, invoice }; } catch (error) { return { ok: false, message: failure(error, "The invoice could not be updated.") }; } }
export async function finalizeInvoiceAction(id: string): Promise<InvoiceActionResult> { const parsed = idSchema.safeParse(id); if (!parsed.success) return { ok: false, message: "The invoice identifier was invalid." }; try { const invoice = await finalizeInvoice(await manager(), parsed.data); revalidatePath("/"); return { ok: true, invoice }; } catch (error) { return { ok: false, message: failure(error, "The invoice could not be finalized.") }; } }
export async function deleteInvoiceAction(id: string): Promise<DeleteActionResult> { const parsed = idSchema.safeParse(id); if (!parsed.success) return { ok: false, message: "The invoice identifier was invalid." }; try { const deletedId = await deleteInvoice(await manager(), parsed.data); revalidatePath("/"); return { ok: true, id: deletedId }; } catch (error) { return { ok: false, message: failure(error, "The invoice could not be deleted.") }; } }
